// ig message catalogue. Plain JS object so it loads under Vite, raw
// Node and Vercel's bundler alike (JSON imports need an import attribute
// that not every loader supplies). Edit values only; keep the key set.
export default {
  "_note": "Igbo translations for NGScorecard's UI. Populated in Phase 3+ against the key set in en.json. Any key absent here falls back to the English value. Not natively reviewed until marked otherwise in docs/i18n-plan.md."
}
